{
  "id": "61129f76",
  "title": "V4: orb host self-subscribe to daemon audio.level + rms mapping",
  "tags": [
    "v4",
    "rust",
    "commit"
  ],
  "status": "open",
  "created_at": "2026-06-16T21:05:50.009Z"
}

V4: orb host self-subscribes to daemon audio.level. RustUiShellController passes FAE_DAEMON_SOCKET + FAE_DAEMON_TOKEN_PATH env to fae-ui-shell. fae-ui-shell main.rs: background subscriber thread → auth → subscribe → read loop; on audio.level map rms→shader range (x=clamp(rms/0.40,0,1); mapped=0.12+sqrt(x)*0.48) via event-loop proxy set_audio(Some); on playback_ended set_audio(None). Validate: Rust fmt/clippy/check/test + live flag-ON (orb rides real voice; barge-in cuts both). Commit feat(orb): subscribe to daemon audio levels.
